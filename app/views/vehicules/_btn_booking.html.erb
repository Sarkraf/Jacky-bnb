<% if current_user != vehicule.user %>
  <%= link_to "Réserver", new_vehicule_booking_path(@vehicule), class: "btn-request link_to" %>
<% end %>

<%# use with <%= render "btn_booking", vehicule: @vehicule %>
